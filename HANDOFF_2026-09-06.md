# Resume here — 2026-09-06 background checkpoint

## Current — selected Station, native ground proof

User said yes to the middle Station bridge. Exact upper pixels frozen; no new
generation this turn. Optional `-WastesStationBridgeDirectory` in the isolated
builder audits and overrides ONLY the existing named-SP ground gallery, never
the full-depth Station. Fresh0warning/0error build; native ground pairs beside
garage/shell/checkpoint and normal return pass. Screenshots/trace/limits:
`Art/Validation/WastesStationBridge-2026-09-06/README.md`.

Installed QA mod hash1CBA441BE9DCF30507F5D6653A05B8B0366FDC48C9B9CAB6837B3D1D5CF48CF6.
Named QA world saved and client verified at main menu22:04:21 local. No regular
world, repository Content or game C# change. Same old grove digest mismatch,
unresolved and never bypassed. Backup and build mirror paths in evidence.

Current512x460 art explicitly fails the full-depth1408px module gate. Next:
authored lower cliff/side continuation preserving approved upper, explicit
gallery-to-full-module soil/socket mapping, then camera/lighting/real-biome
checks. Do not approve whole Wastes family from these ground views or ask for
the same style decision again. No further whole-building redraws. Keep #11 open.

## Earlier — Station/garage style bridge, offline only

Latest user distinguishes finer garage texture/preferred contour from broader
Station pixels/rough cutout. Garage design is liked; prior pair is not accepted
for cohesion. Earlier blanket "keep Station as the fixed reference" is qualified.
Preserve both originals. One Station edit (not another garage redraw) is in
`Art/Candidates/WastesStationBridge-v1/`. See its README and light/dark comparison.
Generated source failed requested dimensions/alpha; retain it honestly. Reviewed
mask + recorded RGB donor preparation + exact RGBA export remove painted matte.
Explicit3/8 nearest-center fitting is an offline study, not lossless/runtime art.
Agent sees broader canopy chips and connected branches, with still-detailed
terrain. Do not claim a perfect match or auto-promote it. User review pending.

No native client/session/world/build/installed-mod change this turn. Previous
2177b8d QA build evidence below still applies only to the old pair. Client was
last verified at main menu then; this turn did not inspect/control it. No new
game screenshot. Wayfinder #11 stays open. Next is candidate review, then deep
foundations and native validation if selected, not another regeneration loop.
Mask tool's no-decision empty-array/null failure was fixed and regression-tested.
Source/export/donor/sample provenance and three actual CLI defect checks pass.
Keep unrelated localization and research edits out of this work's Git staging.

## Earlier — component assembly and native ground evidence

User said go on v1's cleaner BUILDING with rougher v2 TERRAIN. Implemented as
`Art/Candidates/WastesMidRuins-v2/ComponentAssembly-v1/` using exact source pixels
and a stepped footing join, no image generation or repainting. Station unchanged.
New generator/independent validator reproduce the asset and reject three actual
CLI defects. Ordered JSON fixed the initial record-only reproducibility failure.

Isolated build passed0 warnings/errors; optional assembly override affects only
the temporary ground-gallery pack. No repository Content PNG or game C# change.
Fresh native depot/shell/checkpoint captures and normal return are in
`Art/Validation/WastesDepotAssembly-2026-09-06/README.md`. All paired draw/release
checks and four existing live-validator defect controls pass. Art approval is
still pending; this does not claim Station style match or full-height coverage.

The installed QA mod hash starts66943139… (full hash in evidence). Saved only
the disposable Apogee Native Visual V3 after the tests. Old grove checkpoint
mismatch remains; never reset it to force a passing rebuild. No regular world
opened. Two unrelated dirty localization/research files stay excluded from Git.
Next: user reviews this exact combination; if accepted, deeper foundations and
ordinary placement. No more whole-building redraw loop or extra biome work.

## Earlier decision — v1 building, rougher terrain direction

Supersedes the blanket v1 rejection below. User prefers the previous cleaner
depot building between the candidates, but likes grittier treatment for terrain.
This is a working component preference, NOT a claim that either building matches
Station. No more whole-building generation this turn. Station remains unchanged.
V1's building is preferred; v2's building is not selected. A combined asset does
not yet exist. Future assembly must align the ground/footing seam and receive
native evidence before installation. No PNG, code, installed mod or world change
in this decision-only turn; update Bible/Wayfinder/status without promoting art.

## Earlier — grit must remain; clean v1 rejected

The user clarifies that Station is gritty and that the cleaner depot lost the
wasteland feel. One v2 material edit restores chipped paint/soot/masonry. At
native-size review it remains finely textured, so a separate two-pixel-grid
resampling study is shown honestly as an experiment, not new authored detail.
Read `Art/Candidates/WastesMidRuins-v2/PixelStyle-v2/README.md` and both boards.
Do not claim the style is accepted or automatically regenerate another version.

Reused mask/study tools now have pinned v1/v2 recipes. Both old/new outputs and
grid study reproduce exactly; pixel audit and five reason-checked defect CLI
runs pass. Station, Far and Close stay unchanged. No native client, mod build,
world or renderer changed. Current boundary is art review, not implementation.
V1 is explicitly rejected; v2 review and all native/deep module gates remain.

## Latest — native size retained; ruin pixel language revised

Supersedes the awaiting-size-review instruction below. User likes the overall
look but says the ruins are too realistic/fine-grained compared with Station.
One motor-depot style-transfer redraw is prepared for review, not installed:
`Art/Candidates/WastesMidRuins-v2/PixelStyle-v1/README.md`.
It uses broader concrete/rust/shadow shapes at approximately the old building
size. Existing Station is unchanged. Do not restyle all three before feedback.

One generated edit returned1317x1194 opaque checkerboard, not requested size or
alpha. Existing reviewed-mask workflow creates real alpha;3241 bounded edge RGB
donors are recorded. Explicit1/3 fitting gives a512x432 upper-only comparison.
Independent pixel audit, four reason-checked CLI defect controls and exact
fresh-directory reproduction pass. No build, live test, mod replacement or
world operation this turn. Old native size proof does not approve this redraw.

Next: user judges the revised depot's art against the unchanged Station. If
accepted, transfer that language to the two existing ruin shapes, then correct
native QA assembly/deep foundations and normal spacing. Not an invitation to
make more unrelated concepts. Full Wastes gates and starting-area/Maw order
remain. Two unrelated dirty localization/research files remain excluded.

## Latest — opposite-facing checkpoint and actual in-game scale

Supersedes the older next-step silhouette/offline review below. User accepted
the three basic ruin concepts, asked about matching facing, then could not judge
scale without a character in-game. One bounded checkpoint edit plus reviewed
mask preserves the original cliff and upper-left lighting. Original concepts
remain unchanged. `ScaleStudy-v3` has explicit2/5 upper studies (Station1/1).

Actual native captures, exact build/reproduction and limitations:
`Art/Validation/WastesMidScale-2026-09-06/README.md`.
Three paired ground views in **Apogee Native Visual V3**, playergg,2560x1369:
checkpoint, depot, shell; real player/terrain with Station on the left. Normal
Mid/Close suppressed only in the gallery; ground baseline restored afterwards.
Zero build warnings/errors and submitted-position/dimension failures. Both
static and live audit CLIs reject all four of their respective defect controls.

QA mod SHA256 `D17A0EEFEADA32A1FA255691FDE31EFB219910253CAE7E07646AFD21BB9BA78F`.
Rebuild with all three overrides documented in the proof README. Accepted
Station/Close/Far files unchanged. No ordinary world opened or Content art
promoted. Save/quit validated at20:27:05 local; game left at main menu after Exit
inputs did not close its window. No process force-kill. Backups in local temp
`ApogeanMidScaleQA-67a033dc4ef84d9688257947c7a58ef3`.

**Next:** await user size/facing feedback on the actual captures, not the old
offline board. Do not regenerate more concepts. Once accepted, assemble real
deep foundations and stable quiet-spaced placement; full flight/lighting/
real-biome/restoration/residency tests are still required before promotion.
These512x460 gallery sprites are not1408px-deep modules. Keep Wastes → starting
area/drop pod → shallow Maw order and the green-survivor Jungle brief.

Broad Background gate rerun remains RED for old source dimensions/final-row
stretch; other constituent static stages pass. Existing grove reload digest
mismatch remains unresolved; no checkpoint reset/rebuild. Machine state tracks
`wastes-mid-ruins-scale-v1` as fixture-pass, user review/production pending.
Preserve unrelated dirty localization and world-systems research files.

## Latest follow-up — three Mid ruins and living Jungle direction

This supersedes first-concept rejection/parking notes in the historical checkpoint
below, not the earlier Space proof or unresolved production gates. The user likes
the broken building and asks for two or three distinct ruins. Kept it; generated
exactly two new concepts: motor depot and checkpoint. All three now have real
alpha derivatives with recorded mask/edge-color preparation under
`Art/Candidates/WastesMidRuins-v2/`.

- Original checkerboard PNGs unchanged. Export/provenance checks pass for all
  three, with9 in-memory controls plus the existing exporter's11 actual CLI
  rejection cases. Light/dark previews inspected; these are offline views.
- Depot/shell exceed current Mid widths; checkpoint storey scale also needs
  fitting against Station. Do not install as-is. Next art gate: two new shapes,
  then measured native-scale assembly and stable quiet-spaced placement. No
  more unrequested concept generation.
- Bible and `BACKGROUND_BIOME_DIRECTION.md`: Kessler Wastes/forest history,
  Helix infection research ruins, snow/desert transport remains, Sentrix sky/space.
  Historical architecture is not gameplay ownership or infection-origin lore.
- **Newest user clarification:** Jungle is the lush green survivor, living canopy
  reclaiming abandoned Helix sites. No desolate Wastes recolor or new combat/
  evil rules. Other biome briefs remain separate.
- No runtime code, Content PNG, installed mod or world changed. No native client
  launched this follow-up. Accepted Station/Highway/Far/Close preserved.
- Order remains bounded Wastes baseline → starting area/drop pod → shallow Maw.
  Jungle direction is specified for its future pass, not promoted now.

## Earlier Space checkpoint (historical)

The user authorized continuing while at work. No recurring automation or new
task was created. Focus remained on the agreed Space-handoff fix, native proof
and one additional Mid-building concept; no dependent Maw/faction expansion.
The disposable world was saved/validated, client closed normally and original
2560x1369 window dimensions restored after the1080p run.

## Completed, with bounded evidence

- `WastesCameraProjection.LandOpacity` fixes the QA city persisting into Space.
  Full submitted color gets a smooth envelope; either player or camera Space
  guarantees0. Mid staging and regional Close anchoring are preserved. The70%
  Far onset is our policy, not an engine rule. No new underground cutoff.
-18180 actual-policy checks; red old behavior first, then green. Real CLI
  controls reject always-visible/hidden, abrupt and camera/player-blind code.
- New camera cases `space-fade`, `space-edge`, `space-ascent`, `space-descent`.
  Native ground/fade/entry/high-Space and both full continuous-flight directions
  pass at2560x1369 and1920x1080. Actual submitted-alpha logs and their fault
  controls prevent geometry-only false success. Native PNGs are unretouched.
- Exact approved Station and current Close-v2/Far runtime PNGs did not change.
  Build0warnings/errors; installed QA mod SHA256
  `6B2F8A1D4796A3D92E40AD76BD38A6EC494C636091866EACB900FAA34F1DE733`.
  Use the same two isolated-build overrides from the proof README; a normal
  build omits the optional candidate art and is not the tested package.
- One1024x1536 Mid ruin concept created with the built-in image tool, original
  and exact prompt saved. **Not installed:** it has unwanted perspective and
  zero alpha transparency. Do not spend a cleanup loop disguising those failures.

Proof: `Art/Validation/WastesSpaceHandoff-2026-09-06/README.md`.
Concept: `Art/Candidates/WastesMidRuin-v1/README.md`.
The Bible, roadmap and AuthoringStatus distinguish the focused fixture-pass from
whole-scene review. Wayfinder issue#11 remains open.

## Next order / stop rule

1. User review of actual fade/ground/Space captures and comfort, not a request
   to redo accepted landscape art. Inspect partial fade, clear Space and return.
2. Decide whether to retain the new Mid ruin's basic shell/foundation design.
   The current angled, opaque draft is not an install candidate. If retained,
   one bounded side-view correction, explicit reviewed mask, native-scale
   upper/cliff/continuation assembly, light/dark and flight tests before install.
   Preserve accepted Station/Highway, gaps and quiet intervals. No Far crop.
3. Close the remaining bounded Wastes baseline gates: exact-candidate real
   biome/restoration/cave transitions and necessary regression matrix. Then the
   starting-area/drop-pod slice, not another open-ended series of variants.
   Do not label an overall background family polished from the Space fix alone.

## Known limits / operational notes

- Broad `Background` content gate remains RED for existing production source
  dimensions and the old generic renderer's final-row stretch. Mask, city-mask,
  Space, restoration-policy and HD-contract stages pass. No threshold weakened.
- Existing grove digest mismatch still reports expected87B951FE… vs actual
  EB1D7019… on load; no rebuild or checkpoint reset occurred. It is a separate
  unresolved vegetation-persistence issue, not a new Space renderer crash.
- No multiplayer, gravity, other-world-size, performance or overall art approval.
  Current raw texture data28.36MiB is not a memory/FPS benchmark.
- User's ordinary worlds were never opened. Camera fixtures change only the
  named disposable SP world's viewpoint and temporary scene state. Existing QA
  file bridge remains the input mechanism. Save/quit through its allow-listed
  `qa-save-and-quit`, verify normal save, then exit via native UI.
- Pre-run backups of old mod/config and disposable world/player are in local
  temp `ApogeanSpaceQA-785a16bc9f2c47a790ca3b1debd3df38`.
- Do not use the original opaque generated concept as runtime input. The
  reusable mask exporter cannot correct its perspective or artistic scaling.
- Original unrelated dirty files must remain excluded from this commit:
  `Localization/en-US_Mods.apogean.hjson`, `RESEARCH_TMODLOADER_WORLD_SYSTEMS.md`.

No sleep/wake continuation is scheduled. Resume on user request.
