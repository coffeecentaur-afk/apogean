# Foreground continuation source

One built-in image-generation edit call after the user authorized depth completion and testing. No CLI/API fallback. Source saved unchanged as `Foreground-Lower-Source.png` (actual1449×1085). Runtime export crops only the final rightmost column, joins with a256-row connected source-pixel cut, and never rescales. See `Tools/Preview-WastesModularMid.ps1` and `Derived/foreground-depth-measurements.json`.

## Exact prompt

Use case: stylized-concept
Asset type: native-resolution lower cliff continuation for a Terraria-style 2D side-view parallax background.
Input image 1 is a material, pixel scale, and horizontal alignment reference. It shows the existing upper foreground bank. Do NOT redraw that bank. Generate ONLY the next lower section of its cliff, to be attached beneath it.
Canvas: 1448 pixels wide by 1086 pixels high, landscape. Full-bleed opaque dark earth and rock, not a floating rock island. This is a lower texture panel, no grass skyline, no sky, no blank borders or captions.
At the TOP of the new panel, closely continue the bottom 256 rows of the reference at the same horizontal positions and the same pixel scale: deep brown eroded soil with charcoal slate outcrops and vertical fractures. Below that overlap, extend the cliff downward with fresh original rock shapes and subdued sparse details for at least another 800 pixels. Keep the top-to-bottom material value consistent enough to join without a bright or dark horizontal band. Broad quiet earth faces between broken charcoal rock slabs; sediment lines are sparse and subordinate. Dark cracks, not bright highlights. Some tiny ochre mineral seams are okay, no glowing lights. The very bottom remains uninterrupted cliff material continuing off-canvas, not a flat ledge or visible bottom edge.
Style must match the input: hard-edged pixel clusters at the SAME scale, clear clustered rock planes, dark muted browns and charcoal grays. Side-on orthographic 2D terrain, no perspective change. No mirroring or repeated identical columns. No grass, trees, buildings, new pipes, props, caves, text, interface, outline around the full image, white specks, gradients, blur, antialiasing, fake checkerboard, photorealism, painterly brush marks. Keep this a quiet lower continuation of the existing cliff.
