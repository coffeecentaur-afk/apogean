# Native city runtime-fit candidate

See `Art/Validation/WastesCityRuntime-2026-09-05/README.md` for exact package,
native screenshots, pixel/geometry tests and the **open Space visibility defect**.

Reproduce to a new directory with `Tools/New-WastesCityRuntime.ps1
-OutputDirectory <new-path>`, then run `Tools/Test-WastesCityRuntime.ps1
-CandidateDirectory <new-path> -ReportPath <new-path>/Runtime-report.json`.
The source hash is pinned. Never overwrite this reviewed candidate.

`Horizontal-Join.png` and `Ground-Continuation.png` are offline1:1 crops, not
game captures. `fit.json` describes the protected source region and quilting
inputs. The runtime report certifies narrow static contracts, not art approval.
Minimum-error cuts select existing pixels; they cannot invent believable new
architecture or remove repetition. Do not reuse this pinned-city fitting script
as an unreviewed universal landscape generator.
