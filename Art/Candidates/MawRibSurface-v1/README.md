# Cortical rib and thick fiber-cap candidate

September 11, 2026. Not user-approved or enabled in ordinary world generation.

The original image-generation source is `source.png`. It asks for a quiet fossil
bone surface rather than separate plates, cracks between tiles or rows of bones.
`Tools/New-MawAnatomyCandidate.ps1` fits a 128×128, sixteen-color material field
and applies the established native Stone masks. The cap uses the existing
approved harsh fiber field for both grass roles; no side-face art is replaced.
Exact input/output hashes and local reference paths are in `Native-v1/recipe.json`.

The existing porous bone remains the structural-mass/root comparison. New rib
geometry is real contiguous solid tiles with native hammered slope endings,
not a stretched sprite or invisible platform. The quieter grain may still read
too horizontally: hold that question for the user's art review.

Run `Tools/Invoke-ApogeanContentGate.ps1 -Profile MawAnatomy` for source/mask,
padding, mapping, broken-input and geometry checks. Native evidence lives in
`Art/Validation/MawAnatomy-2026-09-11/`. Do not regenerate over this bank or rebuild
its saved 100×76 comparison. Reproduce into a new candidate folder.
