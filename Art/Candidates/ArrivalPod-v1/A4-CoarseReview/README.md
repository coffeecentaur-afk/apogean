# A4 coarse style — direction accepted

User: "perfect make it more gritty and more broken along with a landing area
that looks like it actually drop pot impacted the ground".
Preserve the larger clusters and simpler planes. A5 in `../A5-ImpactReview/`
adds that requested damage and impact-scene direction. Neither board is a
native-scale sprite, verified alpha mask, installed texture or game capture.
The existing Native-v1 appearance is superseded, even though its behavior tests
passed. Do not reintroduce its fine speckles during the damage pass.

Built-in image edit; complete prompt in PROMPT.md. Output: design-a4.png.
