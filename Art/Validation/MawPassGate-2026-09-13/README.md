# AB / AC — validator and request-publisher repair

September 13, final-hour preparation. These are offline tool repairs; the game
remains closed after AA's normal save. No new native-render acceptance, textures,
world generation, saved-fixture repair or package installation.

## AB: obsolete checks, not a tree-art regression

The old world-integrity gate demanded the exact text of a 28-value ValidateSet.
The actual sender retains those 28 cases and has 13 legitimate additions. A
comment could satisfy the old substring check while the real parameter differed.
The new helper parses the top-level parameter AST without executing the script.
It requires literal, unique values and every original case; order/whitespace and
additional cases are permitted. Nondefault attribute options need a new explicit
contract rather than silently changing validation semantics.

The real gate failed the new test before repair. Current, old and reordered-plus-
extended declarations pass; 34 malformed controls are rejected. A separate
single-value case isolates rejection of IgnoreCase=false from missing-case checks.
Independent review caught the same masking risk in the duplicate-value case.
It now duplicates a value in the complete declaration. The full suite rejects
an in-memory helper mutant with duplicate rejection disabled. The first replay
attempt lacked PSScriptRoot; rerunning with the explicit tool root established
the intended failure. No repository helper was mutated.

The generic eight-color trunk requirement predates the intentionally muted,
user-reviewed five-color Snapped A v3. All three production sheets still match
that exact candidate. The gate now delegates to the native topology/socket and
Snapped revision checks and pins all three reviewed hashes. Actual CLI tests
accept current v3 and reject historical v1/v2. No pixels were edited or approved
by changing a test. This does NOT repair the older native saved-grove mismatch.

## AC: publish only closed complete commands

The old LiveValidation sender actually overwrote an isolated pending
qa-perf-snapshot with conversion. Failure scratch is retained at
`C:/Users/max_h/AppData/Local/Temp/ApogeanLegacySenders-38ed144c5c26456d8634bb4e4dd1a191/0`.
The existing Windows file-sharing reproduction also rejects a read while the
old request writer is still open. Neither test used the real game queue.

Ten legacy wrappers now use Publish-QARequest. It writes a uniquely owned staging
file, closes it, then renames without overwrite in the same directory. Defaults
and command payloads stay unchanged; the two separate arrival/entrance queues
are explicitly allowlisted. Arbitrary filenames or paths are refused. Receivers
and their player/world restrictions are unchanged.

Twelve actual wrapper cases preserve a pending marker, publish the exact payload
when free and leave no staging file. Tests cover both special save-and-quit
mappings, the separate queues, queue isolation and three invalid paths. A source
sweep refuses future Request*.ps1 wrappers that bypass the publisher. This is
filesystem-level safety, not an acknowledgement that the game consumed a request.
Successful scratch: `ApogeanLegacySenders-cf40978d019e4d4cbf58da6f61b61f7b` in Temp.

## Retained final regressions

`ac-archived-native-replay.txt` replays two AA icon reports, two Y binding reports,
13 W/Z growth reports and five V climb reports through current independent
validators. It preserves the interrupted growth/climb verdicts as incomplete;
valid report accounting does not turn a partial run into a gameplay pass.

`ac-earlier-native-replay.txt` additionally replays five O timing reports, four P
conversion reports, both incomplete S returns and the T18HP entry. Across the
two replay logs, 34 raw reports remain internally consistent; that is not34
successful complete gameplay tests.

`ac-all-gate.txt` records the driver and final verdict, but Start-Transcript
omitted nested child-process output. It is retained as incomplete logging, not
called a full transcript. That run finished with zero compile warnings/errors
and one failing script: BackgroundProductionReadiness. Its generic HD V0 asset
size and authored lower-coverage requirements were not weakened.
`ac-all-complete-output.txt` captures the later child stdout/stderr directly;
it finishes with79 of80 scripts passing, only BackgroundProductionReadiness
RED, and compile-only0 warnings/0 errors. This logging correction did not rerun
native game tests or promote art. The native tree-reference requirement remains
local to the existing Captures/ApogeanTileLabReferences workflow; this is not a
new clean-machine CI portability claim.

At03:48UTC, `ac-final-save-tags.json` independently rereads the pinned native
TagIO data: all16 existing records still match pre-AA. Installed AA package
2450BDD2 is unchanged, the real queue is empty and no Terraria window is open.

No stage advance is implied: art, natural-light readability, manual interaction,
whole-biome generation, minimum hardware and multiplayer retain their own gates.
