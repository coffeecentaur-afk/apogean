# Seed-aware shallow Maw evidence — September13

This is a **contracted, QA-only candidate**, not an approved production biome.
See MAW_SEED_INTEGRATION_2026-09-13.md and HANDOFF_MAW_SEED_2026-09-13.md at the
repository root for package, raw local log locations, exact scope and next work.

- `MawSeed-plan-first.json` and `MawSeed-plan-second.json` retain the initial
  failing plan trials. `MawSeed-plan-final.json` has292 passing layouts across128
  distinct seeds plus31 corrupted layouts and9 invalid inputs correctly rejected.
- `MawSeed-layout-101*.svg` and the PNG are geometry diagrams, NOT engine renders.
  A passing pure seed101 plan still fails native protected-site search. Do not
  mistake planner evidence for native world generation.
- `MawSeed-native-summary.json` transcribes the scoped native results and pins
  the full local log hashes. Seed202 passes twice with matching terrain digest,
  then passes real client save/reload and pristine inspection.101/303 fail site
  search;404 fails an unowned-state framing assertion. Failures remain failures.
- `MawSeed-rib-inspection-2026-09-13.png` is an unedited native viewport capture,
  with a local diagnostic lamp ON. Only nearby terrain is readable; this is not
  a fully illuminated overview, natural-light approval or pixel-coverage proof.
- `MawSeed-entrance-natural-2026-09-13.png` uses natural lighting. The left old
  ice-material shell and bright fiber-edge gaps are known unresolved defects.
  Native photo mode also shows black/unlit top areas and a different scenery
  composition from the normal game viewport. This image is terrain evidence,
  NOT a background-routing/render pass or a presentation-quality screenshot.
  No new art approval is claimed. User resource packs remained enabled.

The original V3 MS Paint mockup was preserved. New worldgen is enabled only by
the exact fresh QA world name and transient native generation identity latch.
Never rebaseline failed/unowned state, weaken real traps/structure protection,
or modify the accepted mockup to force this test green. Room cache remains empty;
Node site remains a reservation. Manual traversal, combat, full depth, scenery,
multiplayer, broad compatibility and minimum hardware are not certified here.
