# Maw anatomy — September 11 evening

September11 evening implementation checkpoint. Native scope: `gg`, single player,
`Apogee Native Visual V3` only. Ordinary worlds and the Gullet generator are
unchanged. Do not rebuild the old fiber study or this saved comparison.

Start next time with [NEXT_SESSION.md](NEXT_SESSION.md). Review captures:
[rib/cap v1](native-after-joins.png), [rib v2](rib-v2-native.png),
[hanging fibers before the recorded cut](hanging-native.png),
[amber awake](amber-awake-native.png), [amber dormant](amber-dormant-native.png).
These are unedited native captures, not concept art or production acceptance.

## Proven so far

- Root diagnosis copied four real neighborhoods into an empty scratch pad.
  `root-before.txt` and `root-flags-before.json` preserve the red evidence.
  Reciprocal material pairs alone did not fix soil joins. The minimal installed
  change is bone's native dirt-merge flag plus reciprocal actual MawGrass merge.
  It is gated to the packed QA build; fallback art is not implicitly certified.
- `regression-after-joins.json`: 24 native base-alpha comparisons pass.
  Deliberately disabling dirt merging exposes defects in seven samples;
  disabling the grass pair exposes six. Rules and scratch are restored.
  This measures base alpha, not the extra grass overlay or complete aesthetics.
- Existing six-request playable suite passes again, including 449 properties,
  sand settling/cleanup, seams, and eight deliberately corrupted cells.
- Separate anatomy comparison at `7396,80`, 100×76 tiles:
  `B510E9D585CAC1CE7E9FED527E5FB8DDFA9DC211CB8D0105FE67DF91209F7FD9`.
  All 7,600 cells, 14 slopes and 378 unsafe wall cells survive an actual
  save/reopen. Its camera is frozen: no traversal claim.
- Native compiler checks replay 4,976,640 rib and 36,495,360 cap pixels.
  Five actual CLI mutation controls pass (positive, rib/cap alpha, guard, index).
  Pure geometry checks include a deliberately disconnected rib shaft.

## Art assessment, not user approval

The old porous bone remains available for masses and roots. The new quieter
cortical field follows the same native masks and has a continuous tapered tile
shaft. The surface is less perforated, but its horizontal grain and its join to
porous roots still need the user's art judgment; do not call it final bone art.
The full fiber-cap candidate sits over the original rooted soil and plain soil.
Liked side-face growth is unchanged. Six existing matching unsafe wall swatches
are displayed away from roots, so they cannot conceal attachment defects.

The later v2 bank uses a separate original chalky source and the same masks,
geometry and fiber cap. Its native capture `rib-v2-native.png` removes the
horizontal material grain, but is noticeably pale and still needs silhouette,
root-transition and tone judgment. Both versions remain candidates. Native
lighting's tile-sized steps are visible; do not attribute every band to art
or flatten engine lighting to make a promotional screenshot look smoother.

Unedited native captures are preserved here as `native-before.png` and
`native-after-joins.png`. The same images remain in the tModLoader Captures
folder, dated233619 and234839UTC. Neither image is a traversal acceptance test.

## Native anatomy physics

308 assertions pass, including294 actual `Collision.SlopeCollision` comparisons
against native Stone across full, half and four slope shapes. Turning off rib
solidity deliberately fails36 comparisons. Scratch restores. This proves the
native collision contract, not a playable descent or hammer/pickaxe animation.

## Hanging fiber — saved exhibit remains RED

The original generated cord was masked, uniformly fitted and compiled into a
16×96 transparent strand with six16px frames/eight colors. No wind-splitting,
rope flag, damage, drops or automatic growth timer. It is a smaller candidate
than the research note's proposed ten-segment production contract.

The first package omitted registration because it checked the PNG extension
after packing; the asset can be transcoded. The build now validates the entire
candidate set before registering its types. The red log is retained in
`hanging-registration-red.json`, not erased by the successful rebuild.

Seven roots at7396,176 matured to42 connected segments. `hanging-native.png`
shows that initial native result.180 initial lifecycle checks passed. After
that capture, root3 at7440,180 retained only depths1–3; depths4–6 disappeared.
Audit shows dry, valid, unactuated support and correctly framed remaining
prefix. That does NOT establish the cause. Keep the saved scene; never rebuild
it or redefine its full42-segment baseline as39.

Expanded scratch-only tests pass212 native checks: two substrate/six shape
matrix, top/middle/tip cuts, root loss/purification/actuation, supported Maw
conversion, paint/coatings, length/gap constraints, wet-growth rejection and
actual `Liquid.AddWater` contact. Lava removes the contacted segment and suffix;
water/honey/shimmer do not. The entire2000-cell before/after fingerprint is
unchanged by the scratch tests. This does not clear the red saved exhibit.
Evidence: `hanging-lifecycle-first-pass.json` and
`hanging-probe-and-amber-placement-red.json`. Passive cut-callsite logging is
enabled for the saved roots, excluding deliberate scratch cuts.

Matching native-binary research found that Solar retaliation/dash can cut
`tileCut` fibers; camera capture can also trigger section framing, but no
native capture-to-Solar attack path was found. Neither establishes this cut's
cause. See the vine research follow-up. Keep the red specimen and exact events.

## Matching walls and amber emission

Six unsafe wall swatches use the accepted material bank. New amber terrain
and unsafe-wall sibling types reuse the exact existing amber textures; no
second large texture bank. Build-only emitter tables count explicit amber
palette roles against native opaque masks. Normal soil/bone/fiber stay quiet.
Lookup checks785 assertions/six malformed-input cases; the actual compiler CLI
also passes tiny synthetic alpha/gutter/wall-overlap/quiet-source controls.

The first placement envelope and its first alternative were occupied. No
cells were cleared. The third bounded location,7276,216 (100×52), was empty.
Both enclosed chambers and all5200 cells survive actual save/reopen.
470 native checks cover source counts, .32 dormant/awake ratio, hook routing,
scoped preview, unsafe walls and restored echo/actuation/paint/fullbright/solid
cover probes. No progression flag is changed by previewing awake/dormant.

`amber-awake-camera-coverage-red.png` preserves the first screenshot's unlit
lower portion: the camera followed an out-of-room player and did not warm the
whole capture. The corrected diagnostic separates camera from player position.
`amber-awake-native.png` and `amber-dormant-native.png` show the complete same
saved chambers. Native RGB samples at local20,28 are85/48/8 awake and27/15/0
dormant; at70,25 they are95/53/9 and30/17/2. Quantized screen light is not an
exact floating-point ratio. `amber-first-lighting.json` retains the earlier
native results; final session evidence records the corrected full coverage.

These are proof rooms, not new cave scenery or finished architecture. Brightness,
transition timing, Echo Sight policy, sloped-highlight coverage, GPU coating
appearance and multiplayer remain pending. Emitters are cell-level, not
per-pixel rays. No final amber-art/lighting approval is implied.

## Next bounded checks

Review cortical rib texture and the thick fiber transition at native scale;
diagnose the saved strand cut without rebaselining. Then combine accepted
materials with existing easily mined tooth hazards in one new shallow Gullet
proof. Test preparation-dependent traversal before changing the generator and
its old shelf/fall validator together. No manufactured safety staircase,
preplaced rope, invisible spikes or mandatory damage.

## Preserved red / environment

The pre-existing grove cross-reload mismatch remains RED. Only within-request
grove guards pass; no saved hash was rebaselined. Steam automatically updated
Spirit Reforged from 0.2.2.6 to 0.2.2.7 during launch; no dependency was installed
or enabled by this work. Record the actual environment, not blanket modpack
compatibility. The baseline 466 pinned QA assets remain byte-identical.

Final comparison environment enables only Apogean and CheatSheet. Workshop
discovery/update messages are not evidence Spirit was enabled. Startup reports
1.7GB attributed RAM for the whole Apogean QA bank: record this as a profiling
concern, not an incremental measurement of these new materials or a VRAM test.

## Package and regression checkpoints

The corrected amber-camera package was
`FBE570AF3799A997E6262C1D06EBB9EE73D4A0CCD991F26C13265D08933852A4`.
It passed the complete six-request earlier playable suite and24 root joins
again; evidence is `final-fbe-regression.json` / `final-fbe-session.json`.

The later v2 art comparison package is
`CA03D9D09BD3E92A7980B3CF83ECB4F95AB9A0E59E8D5A65389F6B3EE35593C3`.
Build: zero errors/zero warnings, baseline466 assets unchanged. It changes the
candidate bank, not source geometry or the old specimen's reference hash.
All7600 anatomy cells survive reopening;308 native collision assertions pass
again. The v2 atlas independently passes both mask replays and five actual
CLI broken-input controls. `Tools/Invoke-ApogeanContentGate.ps1 -Profile
MawAnatomy` passes its six scripts on the default v1 bank as well.

`final-ca03-session.json` records the later308 anatomy,212 scratch-vine and470
amber checks, plus the preserved shortened strand audit. The current-only
vine fingerprint is41B890DA75DD6AFD982BABD8FE4E22321EBC659B41930793F7951278E7691F01;
this includes raw IDs/frames and is not a portable cross-build baseline.
The missing depth4–6 pattern remains; there was no replacement/re-growth.
Native save completed at19:55:12 America/Chicago, then the game returned to
the menu and exited normally. No regular world was opened.
