# Development handoff — 2026-09-02

## Why the scope was reduced

The full terrain, structure, and world-generation pass was paused after the earlier tiles rendered as repeated squares and the capture camera crashed in `Main.DrawLiquid`. The new workflow proves one material in a disposable client fixture before it is allowed into production. Broad world-generation work remains frozen while this workflow is established.

## Completed today

- Added a live-client vanilla atlas exporter. `/apogean exportatlases` requests `Images/Tiles_0` and `Images/Wall_2` directly from `Main.Assets` and writes the installed dirt tile and natural dirt-wall atlases to `Captures/ApogeanTileLabReferences`.
- Added `Tools/New-WastesSoilCandidate.ps1`. It preserves the live vanilla alpha/framing topology and maps its value ramps to a five-color dry-umber Wastes palette. It does not draw borders around atlas cells.
- Added isolated diagnostic Wastes soil and wall content.
- Expanded the Tile Lab to show vanilla controls and Wastes candidates side by side, including isolated tiles, connected lines, crosses, dense fields, walls, half blocks, slopes, dirt merging, and a water basin.
- Strengthened `Tools/Test-TileLab.ps1` so malformed dimensions, soft alpha, excessive colors, magenta leakage, changed atlas occupancy, or a difference between the tested candidate and promoted production asset fails validation.
- Promoted only `Content/Tiles/WastesSoil.png` and `Content/Walls/WastesDirtWallUnsafe.png` after the client-rendered candidate passed.
- Saved the palette/material concept at `Art/Reference/WastesSoilMaterial-reference-v1.png`. It is visual direction only, not an engine-ready tile atlas.
- Wrapped automatic reference export in error handling so an optional diagnostic export cannot prevent a world from loading.

## What was verified in the real tModLoader client

- Cold-launched tModLoader and loaded the disposable world `Apogee Native Visual V3`.
- The control and candidate suites rendered at normal game zoom.
- The Wastes soil formed continuous connected terrain without the old graph-paper grid.
- The natural Wastes wall remained visually distinct behind solid soil.
- Slopes, half blocks, dirt merges, and the water case rendered correctly.
- The capture-camera probe completed without an exception.
- Probe state was `scene background=-1`, `scene water=-1`, `main water=0`, and `capture water=0`.
- Final capture: `C:\Users\max_h\OneDrive\Documents\My Games\Terraria\tModLoader\Captures\Apogean Tile Lab Capture Probe.png`.

## Static/build results

- `Tools/Test-TileLab.ps1`: PASS.
- `dotnet build apogean.csproj --nologo`: PASS with zero errors. The existing lowercase `apogean` CS8981 warning remains.
- Candidate and production soil PNGs are identical; candidate and production wall PNGs are identical.

## Deliberate limitations

- This is a conservative, native-readable dry soil foundation, not the final signature Wastes terrain family.
- No Wastes grass edge, plants, trees, biome wall-conversion matrix, furniture, corporate tiles, backgrounds, or structure geometry was changed in this slice.
- No Maw or corporate world-generation expansion should resume until each required material passes the same isolated renderer gate.
- Existing edits in `Localization/en-US_Mods.apogean.hjson` and `RESEARCH_TMODLOADER_WORLD_SYSTEMS.md` were treated as user-owned and left out of this commit.

## Start here tomorrow

1. Review the final Tile Lab capture at normal zoom.
2. If the soil foundation is accepted, build only the Wastes grass edge and its merge behavior on top of it.
3. Validate the grass in the Tile Lab before changing trees or world generation.
4. Continue one visible material family at a time: grass edge, ground plants, tree trunk/canopy, then biome conversion rules.

The important result is that Codex now has a repeatable path from a generated/reference material to a real in-client render, screenshot, validation script, and production promotion. Future visual work should use this path instead of relying on source PNG inspection alone.

## Evening continuation — Wastes grass completed

- Exported Terraria's live 288x1980 grass atlas and 468x180 unsafe grass-wall atlas.
- Rebuilt the Wastes grass candidate on that full native topology. This replaces the incorrect generic 288x270 sheet that caused square, incomplete grass framing.
- Added a dedicated side-by-side Grass Lab covering dense fields, openings, flat caps, exposed sides, stairs, half-blocks, and every slope.
- Cold-launched tModLoader, entered `Apogee Native Visual V3`, and verified the Wastes suite in the real renderer beside vanilla controls.
- The automatic capture completed at `C:\Users\max_h\OneDrive\Documents\My Games\Terraria\tModLoader\Captures\Apogean Grass Lab Capture Probe.png` with no renderer exception.
- Promoted the tested candidates to `Content/Tiles/WastesGrass.png` and `Content/Walls/WastesGrassWallUnsafe.png` and added exact candidate-to-production checks.
- Grass spreading, growth, conversion, paint/coating, and world-generation replacement are still intentionally deferred.

The next isolated visual slice is the Wastes ground-plant family, followed by tree trunk/canopy assets. World generation remains frozen until those materials have their own renderer evidence.

## Late continuation — Wastes ground cover completed

- Created a coherent three-object Wastes vegetation family: four low root tufts, three two-tile bristles, and three two-by-two root shrubs.
- Added deterministic exact-size asset generation in `Tools/New-WastesGroundCover.ps1` and immutable dimension, alpha, palette, occupancy, and alpha-topology checks in `Tools/Test-TileLab.ps1`.
- Added `/apogean vegetationlab` and made `Apogee Native Visual V3` automatically build that fixture for cold-start renderer tests.
- The first real-client pass caught incorrect `StyleMultiplier` usage. Variants are now selected with `WorldGen.PlaceObject(... style: 0, random: variant)`, and the second pass rendered all ten variants correctly.
- Final evidence: `C:\Users\max_h\OneDrive\Documents\My Games\Terraria\tModLoader\Captures\Apogean Vegetation Lab Capture Probe.png`. It excludes entities and cleanly shows all ten variants on the production Wastes substrate.
- Added the validated vegetation family to the existing sparse Wastes surface-decoration pass without changing terrain conversion geometry.
- The full mod build and Tile Lab contract pass. The final client log has no vegetation-lab exception, error, or fatal entry.

Next: use the same isolated workflow for a Terraria-native dead-tree trunk/branch/canopy family before expanding structure or Maw visuals.

## 2026-09-03 revision — concept-derived ground cover

- Rejected the original narrow 1x1, 1x2, and 2x2 vegetation footprints after the live capture made them read as rectangular twigs rather than the approved tangled-root concepts.
- Generated and archived `Art/Reference/WastesGroundCover-reference-v2.png`, then changed the production exporter to derive its silhouettes directly from that sheet instead of synthesizing line primitives.
- Expanded the live objects to 2x1 root piles, 2x3 tall tangles, and 3x2 broad root masses. Their exact atlases are 144x18, 108x54, and 162x36.
- Added explicit two-pixel gutter packing for every 16px tile cell and an exterior-only outline pass that keeps ochre color inside narrow roots.
- Cold-built and rendered all ten variants in `Apogee Native Visual V3`. The final capture contains no split frames, clipped roots, misplaced anchors, or renderer errors.
- `Tools/Test-TileLab.ps1`, `Tools/Test-VisualContracts.ps1`, and the full mod build pass; the existing lowercase `apogean` compiler warning remains the only build warning.
