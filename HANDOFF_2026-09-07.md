# Resume here — 2026-09-07 restoration checkpoint

## Current — September 11 autonomous verification window

Supersedes previous overnight pauses: user authorizes work until19:55 UTC today,
then checkpoint for review. Read Art/Validation/MawNatural-2026-09-11/README.md.
Packed-v2 harsh material gallery is USER ACCEPTED (0158ce6); preserve the art.
Next is a separate mixed natural fixture and actual grass/slope/soil junctions,
then evidence-supported properties. Only gg/V3/SP; never rebuild older galleries
or reset their failed digests. No regular worlds, acid or boss expansion.
Keep unrelated localization/research edits intact. Two-hour heartbeat exists;
do not create duplicates or continue beyond its deadline. Update this checkpoint
with actual results before stopping.

18:35 UTC checkpoint: native mixed fixture X6276Y420W128H104 created ONCE;
3697 draws/946432 alpha pixels/850 wall frames and10 paired grass-soil corners
pass after real save/restart/reopen. Four actual captures in the evidence folder.
Original layout digestC3675BAB... retained; originalwholehash failures diagnosed
as normal vanilla-control growth. Eight real cell mutation controls pass.
Production properties428/441: only13 AmberBolt ordinary terrain/wall dust policy
failures; fix next, then rerun. Native drops/conversion/collision/mining pass.
QA build195C3BD7 installed. Build-CurrentQAPackage pins all466 prior PNG/map bytes
BEFORE packaging; seven asset CLI controls pass. No new art/generation promoted.
Still working until19:55 UTC; heartbeat existing. Do NOT rebuild any gallery.

## Current — September9: v4 native curves and persistence verified

Read Art/Validation/MawToothCluster-2026-09-09/PlayerCurves-v4/README.md FIRST.
Installed C03DAA74 package, clean full build,619 native API checks/32768 contacts,
eight saved variants intact on real reopen. Manual mouse/MP/art remain pending.
Only gg/V3; no old gallery rebuilt. Historical orientation-reload fails because
5866,528 is empty; actual extra cluster at5878,528. Audit leaves it untouched.
Use orientation-audit/curves/proof-view; NEVER orientation-build. Separate saved
proof in right half of same room. Known grove digest mismatch unchanged/RED.
415 other Content PNGs identical. ALL previous art overrides still required.
Next: ONE simpler tooth/material proof, no batch installation before review.
Do not stage unrelated localization/research. Wayfinder26 remains open.

## Historical — September9: player-directed tooth curves, source-staged ONLY

Rounded BACKS follow facing on floors/ceilings, points opposite. Wall: below=tip
up; above=tip down. Read Art/Candidates/MawToothCluster-v1/Native-v4/README.md.
Actual policy37 checks, eight-bank atlas/contact tests and seven CLI corruptions
pass. Source-only compilation clean (Compile target, no package/install). Native
item-hook/preview matrix prepared, NOT RUN. Installed package still01FF09E0/v3,
previous native evidence Rooted-v3/README.md, commitb521693. No game input/restart
or world change this turn. ALL prior package art overrides must be preserved
on next approved install, changing only cluster directory toNative-v4.
orientation-reload, NEVER rebuild. Old displays/user-mined floor gallery/grove
failure unchanged; native persistence/manual/MP still pending. Tooth detail and
leaf-like Maw material are now rejected: next one simpler native-scale proof,
not a full batch. Individual models kept; final placement/physics still open.
Do not stage unrelated dirty localization/research. Wayfinder26 stays open.

## Latest — September9: four-face tooth cluster passes native fixture

User accepted individual teeth and cluster appearance, wants BOTH retained and
floor/ceiling/either solid-wall attachments. Implemented same recoverable item,
non-solid thorn-style exact-pixel contact, easy whole-object mining. No redraw.
Read Art/Validation/MawToothCluster-2026-09-09/README.md FIRST for screenshots,
full build flags and limits. Current installed package137C0151, clean build;
415 other PNGs unchanged.202 checks +16384 native contact comparisons pass
before/after actual save/reload. Ceiling-gap regression caught via screenshot:
TileLoader uses base draw offset, now corrected per placed alternate.77 socket
pixels vs rejected0. Keep negative screenshot; nighttime unlit sample is dark.
Four-way room exists at5856,500 in gg/V3/SP: orientation-reload/day, NEVER build.
Old floor gallery has a user-mined specimen; preserve it. All six new displays
survive reload. Current view left in orientation room for user inspection.
Grove mismatch remains RED with actualBFE03BFD unchanged; never rebaseline.
Individual art acceptance is recorded but final individual physics still open.
Manual placement/multiplayer and mixed-mouth generation remain pending. No safe
ledges, acid/mob expansion, regular-world changes or broad art rebuild. Wayfinder
#26 remains open; next review attachments, then individual physics/distribution.

## Latest — September9: tooth family now available for native appearance review

User needs to see whether the art is too detailed IN GAME. Do not repeat the
offline-approval loop or redraw before that verdict. Exact three sprites now
render through native TileObjectData in a harmless/nonsolid QA-only gallery.
Read `Art/Validation/MawToothFamily-2026-09-09/README.md` for captures, limits,
all required build flags and backup location. Installed packageD9F8581F; all411
prior Content PNGs unchanged plus3 optional atlases. Build0warnings/errors.
Eight specimens at5296,500 in gg/V3/SP;52 native art checks pass before/after
actual save/reload. Gallery already exists: use Request-MawToothArtValidation
reload/day, NOT build. No occupied terrain or old fixtures cleared.
Grove reload mismatch stays RED, actual97024EEE4A unchanged across reload;
never rebaseline it. Candidate-only appearance is not solid/hurt/mining proof.
User detail review remains open, then matching physics, then mouth integration.
Do not start acid/mobs/bosses, remove no-safe-ledges, or touch regular worlds.
Older "not installed" statements below are historical; retain the evidence.

## Latest — September9: three-tooth family, art review pending

User approves short/long/wide direction. Read
`Art/Candidates/MawToothFamily-v1/README.md` and show Native-v1/review.png.
Exact16x32/16x48/32x64 canvases, shared8-color palette; long unchanged.
New short/wide checkerboards removed through inspected explicit masks.
Three static CLI positives and21 rejected defects pass. Empty-mask and missed-
wide-tip failures retained. No build/install/game/world changes; installed
6A5670B5 remains. Art atlases are NOT old four-way runtime replacements.
Next: user shape review, matching physics proof, then provisional50/35/15
seeded rooted clusters. No invisible collider, wedge reshaping or safe ledges.
Preserve accepted art, unrelated dirty files and known grove failure.
Wayfinder #26 stays open; do not advance to acid/entities ahead of this gate.

## Latest — September9: narrower tooth art, not installed

User rejects v1 native fang as a broken-looking broad fragment and authorizes
one slim intact redraw. Read `Art/Candidates/MawTooth-v2/README.md`. Review uses
exact16x48 canvas pixels (9px opaque width), continuous shaft/tip,8 colors,
bone-root assembly and42px ruler. Alpha comes from new built-in source, uniform
sampling/palette fit, then the existing hard-mask exporter; no wedge warping.
Seven actual CLI negative controls pass. Old v1 is rejected visually; retain
its mechanical proof. V2 remains art-only, review pending; old runtime54x216
atlas is incompatible with the new18x54 upright art study. Never auto-install it.
No client restart/build/world change; package6A5670B5 remains. Next: shape review,
then matching native physics probe without invisible rectangular collision.
Preserve unresolved grove failure, unrelated dirty files and all accepted art.

## Latest — September9: one native fang prototype

User said "you may go". Implemented only the next agreed tooth slice. Read
`Art/Validation/MawFang-2026-09-09/README.md` and candidate parent README/contract.
Candidate-only four-way solid fang; native tests pass including199 checks after
reload. Final package6A5670B5, build0warnings/errors. All accepted Wastes/pod/bone
build options retained; add the new MawFangCandidateDirectory override too.
Saved fixture46x30 at4956,500 in gg / Apogee Native Visual V3. Never rebuild it
or the preserved grove automatically. Native art review and actual player
traversal remain; no whole-Maw/production/multiplayer approval. Preserve existing
grove reload failure (candidate digestE0036087), unrelated dirty localization/
world-systems research files, and all accepted art. No third-party dependency.
Next is appearance review/real traversal, then the approved mouth integration;
not acid/mobs/full boss research. Older sections below are historical.

## Latest — September9: tooling research, no runtime changes

Read `RESEARCH_AUTHORING_TOOLING_2026-09-09.md` and its named-library companion.
The recommendation is one internally modular content mod now, small improvements
to accepted-asset build provenance and native QA, and dependencies only after a
specific feasibility gate. These tools are proposals, not newly implemented.
Boss source/video analysis is recorded for later; no boss videos were viewed.
No new library, runtime edit, build, world edit or live test in this research
pass. The installed QA package and preceding bone evidence remain unchanged.

User agrees to solid, recognizable tooth/fang assets with contact damage and
starter-pick removal, not a bone-textured square or drop-through platform.
Next remains the native shape/collision/hazard fixture, then mouth integration.
Check visible shape and solid occupancy separately; inspect native Spike framing
before choosing a layout. See `MAW_ENTRANCE_CONTRACT.md`. Do not reopen accepted
Wastes/pod work or branch into mobs/acid/full encounter implementation.

## Current — September9: approved quiet bone tested natively

User: "i like that more yes you may make all assets and test their properties in
game if that helps design". Do not re-ask v2 offline approval. Localized slate
tissue remains approved. Only structural bone was implemented/tested in this
session; do not claim teeth or all Maw assets complete.

`MawBoneLab` opt-in fixture requires single-player gg in Apogee Native Visual V3.
Saved80x38 bounds4836,500 occupy a surveyed empty envelope, away from the grove.
Native Stone/bone/Mawstone seam, edges, slopes, half-block, paint and actuation
are shown without a custom tile renderer.13 native API checks pass twice, before
and after an actual save/reload. Native screenshots and exact limits are in
`Art/Validation/MawBone-2026-09-09/README.md`. The known grove reload mismatch is
still RED, identical before/after; never rebuild or rebase it to hide failure.

Installed QA package6493031F contains approved MaskedNative-v2; all409 other PNGs
match the accepted package. Production Content/OssuaryBone remains unchanged.
Always preserve the complete accepted background/pod overrides when rebuilding;
the evidence README has the exact invocation. Bare builds omit accepted extras.
`Tools/Request-MawBoneValidation.ps1` refuses to overwrite pending requests;
focus Terraria for consumption and verify log/result before queuing another.

Next: separate easily mined hazardous teeth; native Spike topology must be
observed, not guessed from the288x270 connected-bone atlas. Starter-pick removal,
four orientations, attachment/rope clearance, actual hurt/invulnerability and
save/reload need bounded proof. No safe ledges/preplaced ropes. Follow with
approved mouth integration, changing legacy shelves/fall rule together; keep
the Stomach, Wastes/pod, regular worlds, optional acid#27 and entities untouched.

The entries below are historical and do not override this checkpoint.

## Latest — September9: slate tissue approved; quieter bone candidate

User authorizes the explicit-mask path for terrain and wants grey/black/brown/
amber retained with more varied deeper mutations. PDF pages rechecked against
the unchanged source hash. Do not re-ask the layout or reinterpret the yellow
basins as solid blocks. `Art/Candidates/MawBone-v1/DepthDirection-v1` is one
original material-direction board. User now approves localized cooler slate
tissue; do not re-ask it. Chalky casings/rusty lower material remain proposals,
not new lore/ores/mobs. Approval of that tissue does not approve all bone pixels.

`Tools/New-MaskedTerrainCandidate.ps1` prepares an original source with explicitly
lossy sampling/palette fitting; native Stone contributes ONLY alpha topology.
The existing cutout exporter is reused unchanged. MaskedNative-v2 uses a quieter
built-in edit of the original source, with five fitted colors and the SAME
native alpha/compiler. The actual1x/2x comparison is saved in that directory.
All11 workflow checks pass for BOTH v2 and the retained v1 regression. The new
art still needs user review/native proof. Next: actual-texture review, native
bone/tooth fixture, then mouth integration. Do not confuse the depth board or
technical mask proof with accepted game art. No Content/package/world changes.
Optional acid #27, future entities, accepted Wastes/pod/divot remain untouched.

## Latest — September9: mouth layout approved; bone material review

User saysyes to the raised51-tooth-group layout. Working solid rims24/22 tiles
above ground, no safe ledges, player rope/tooth-mining. Do not re-ask composition.
`Art/Candidates/MawBone-v1/README.md` contains the new material study and exact
prompt. It is not installable:63,498 colors and opaque backdrop; exact native
pixel grid/player scale were not delivered. Ask for material direction only.
Separate palette-mapped native Stone diagnostic preserves288x270 native alpha;
it is technical proof, not the final bone artwork.12 real-CLI controls pass.
Production OssuaryBone remains the known square-frame RED baseline. No new
Content textures, runtime changes, build, installed package or worlds changed.
Next: approved material into native frames and one bone/tooth native fixture,
then geometry integration. Keep #26 open; acid #27 and accepted Wastes/pod frozen.

## Latest — September8: user's Maw sketch and four creature references

Newest revision: user wants a ton of teeth and much higher opening "lips",
like a Sarlacc pit. Candidate now has51 attached tooth groups (was8), with
solid rim crests24/22 tiles above local ground (was6/4). Canvas120x112 adds
24 rows of headroom; below-ground depth and124/62-cell basins stay unchanged.
Nine negative controls and final geometry pass, including density/rim-height
regressions and the no-safe-shelf rule. Preview inspected736/360 light/dark.
Still geometry-only; no new production art, generator, game package or worlds.
User has not approved the exact counts/heights. Next gate remains shape review,
then focused bone/tooth art and native rope/mining/hazard proof.

Latest correction supersedes the first scale study below: user rejects ALL
safe ledges and specifies rope or breaking teeth. Removed resting shelves and
landing markers from the candidate; steep irregular walls remain. A regression
rejects reintroduced two-tile safe resting shelves. Bible/contract now override
the old ossuary-shelf and safe-fall assumptions. No runtime generator, native
world or package change; those legacy behaviors await actual implementation.

Earlier continuation: built `Art/Candidates/MawEntrance-v1/entrance-layout.json`
and a responsive tile-scale cutaway, not production art. `Tools/Build-MawEntranceLayout.cjs`
validates the 120x88 candidate, two sealed basins (124/62 cells), eight attached
tooth clusters, two dry landings and a dry2x3 clearance path; six broken controls
are rejected. This is neither jump/fall simulation nor a native world test.
Review composition before installation; see candidate README for reproduction.
`Tools/Test-OssuaryBoneAtlas.ps1` is deliberately RED on production bone:
all240 bodies are full16x16 squares, no exterior contours. Native Stone positive
control has111 contoured frames/74 unique masks; no universal-mask claim.
No Content PNG, runtime, generator, installed package or world changed.

Follow-up selects portable acid for base enemy traps, with neutralization then
ordinary Terraria reactions confirmed. Do not resume the superseded fixed-basin
recommendation or literal conversion. Amount neutralized and immunity/balance
remain open. Research addendum is source-only; no acid prototype installed.

Read `MAW_SKETCH_DIRECTION_2026-09-08.md` before the next art/code action.
Both PDF pages were visually inspected. User explicitly clarifies yellow with
orange squiggles is acid WATER (optional if too complex), and exposed teeth
hurt like Dungeon spikes but are easy to mine. Structural supporting bone
remains safe; the old blanket harmless-static-bone rule is superseded.
Open throat, inward teeth and asymmetrical flanking basins guide the next
preview, not the current thin dome. Orange dots/fumes are unconfirmed readings.
The four creature roles are agent proposals; tiers, names and AI not approved.
Preserve PDF locally; no third-party art imported into production or GitHub.
This is design/research only: no package, world, runtime, image or evidence-state
change. Next structural fixture and scale preview precede new mob production.
Real-water feasibility recheck: `RESEARCH_MAW_BASIN_REVISIT_2026-09-08.md`.

## Current — September8: divot accepted; Maw entrance wall repair

User: "it looks great continue". Arrival divot scene review is closed; preserve
Native-v3, generated bowl and accepted Wastes art/motion. Next bounded slice is
`MAW_ENTRANCE_CONTRACT.md`. No new mobs/bosses or broad worldgen rewrite.

Added read-only `MawEntranceLab` / `Request-MawEntranceValidation.ps1`: survey,
surface, shallow; single-player gg in `Apogee Arrival QA ...` only. Exports
241x181 actual-cell masks/counts and existing route-validator result. Finds dry
native floor for teleport; no terrain writer, framing, light injection or
camera override. Turns the previous forced Forest bank off for actual routing.

B2 also has native surface/shallow after captures and read-only survey: plan
A4B3108E, mouth5438,485,4958 structural wall cells, routePASS/maxfall97. No
pre-fix B2 comparison claimed. Both disposable QA worlds saved/exited. Status
gate40families/8skills/ownership and14Wastes atlas checks PASS. Native/grid fix
is fixture-pass only; user review and broader scene gates remain pending.

A3 native before captures reproduced the legacy transparent structural-wall
grid. Minimal regression `Test-MawStructuralWall.ps1` resolves actual Texture
binding: old atlas fails14,988 alpha pixels vs installed dirt-wall topology;
existing Maw dirt atlas passes. Fix: reuse that existing texture through
`MawWallUnsafe.Texture`, preserving wall identity/unsafe properties and saved
terrain. No PNG edits/new art. Old atlas retained as explicit negative control.
Added regression to Terrain gate. This is a readable soil baseline, not final
membrane art. ExampleWall is NOT a universal alpha mask for all natural walls.

Installed package `4718C2EB7712198ED77484A14B80394DE860FF0D97022741A01FA3120A7B5AB4`.
Mirror `C:/Users/max_h/AppData/Local/Temp/ApogeanTmlBuild/fa32ff351f794e4b82a5e00dedf0efe2/apogean`.
Isolated build0warnings/errors; final rebuild copies only the repaired wall
class and diagnostic WallID.None cleanup into the preflighted mirror. All410
Content PNGs match accepted previous3aec mirror. Generation unchanged.

Evidence `Art/Validation/MawEntrance-2026-09-08/README.md`: A3 before/after
surface and shallow native captures. Same standing points after save/reload;
grid gone. Exported collision/bone masks, plan hash and3349 wall-cell count
unchanged. Route passes with111 maxfall and41 Stomach clearance. No movement
playtest claim. Broader Maw remains visibly unfinished: bone rectangles,
large arch/cavity, placeholder scenery. New design needs preview before art
installation. Do not equate this seam fix with Maw completion.

Preserve unrelated dirty Localization/research files and old grove/production
gate failures. Arrival issue25 stays open for its broader release checklist;
current bounded appearance is accepted.

## Historical checkpoint follows

## Current — September8: pod handling accepted, divot ready for scene review

Latest user confirms pickup and placement are good and requests the divot.
Do not redraw Native-v3 or retune accepted smooth background motion.
`ArrivalSitePlanner` + `ArrivalSiteSystem` implement bounded final-Wastes
new-world placement:13-wide/two-deep,7-wide/one-deep,undug fallback;48 attempts;
whole-cover clearing; foreign reservations, trees, native piles, chests, wires,
liquids and actual spawn landing protected. Native object placement + value
rollback; saved outcome never regenerates terrain on load. Candidate-gated.

Current installed package (diagnostic shoulder-height fix only):
`B8EE0FD875802AB5AAD4BAC37A925884A002E441825DF4BEA344FCE53C0F2BC0`.
Mirror: `C:/Users/max_h/AppData/Local/Temp/ApogeanTmlBuild/3aec32f1740f433e8f926bfb108f81a7/apogean`.
All410 images and four generation files hash-identical to the tested package
below;0warnings/errors. View now puts gg above the actual higher bowl shoulder.
Final native A3 screenshot and arrival-only client log are saved as
`Art/Validation/ArrivalDivot-2026-09-08/seed-A-final-native.png` and `.log`.
Actual view confirms standing on the shoulder; pod30/support passes at saved
coordinates. Left gg beside the pod in A3 for the user's divot review.

Generation test package SHA256:
`60372CB0CA6B11E2587EF3FC6B92F9B3FAF85E31485E57DF7079650437938375`.
Mirror: `C:/Users/max_h/AppData/Local/Temp/ApogeanTmlBuild/be4abd6e559c46e5b8f4f052234a1e4e/apogean`.
0warnings/errors; all410 Content PNGs unchanged. Prior package backed up under
`C:/Users/max_h/AppData/Local/Temp/ApogeanDivotBackup-32ebd9f790dc40cf8387f98f2018f7ff`.
561 actual-planner assertions,7 source mutations,1 positive/7 live-log controls
pass. Two fixed fresh Large/Classic seeds pass native postgen on final package:
A3 (840595838):13-wide,2-deep; B2 (1424807740):7-wide,1-deep. Initial no-slot
failures retained; same seeds replayed after each scoped fix. No fishing for a
convenient seed. B2 native view/save/reopen/view passes with same coordinates,
intact30cell pod/support and actual screenshots; no full-world hash claim.

Evidence: `Art/Validation/ArrivalDivot-2026-09-08/README.md`.
Primary Worlds now includes disposable `Apogee Arrival QA A2` (intermediate),
`Apogee Arrival QA A3` (final wide),`Apogee Arrival QA B2` (final compact).
Only gg and these disposable worlds opened. All task-owned headless servers
stopped after save. No ordinary worlds/grove fixture rebuilt, no server mod
compatibility claim, no relay/shrapnel art. `Request-ArrivalSiteValidation.ps1`
supports view/save-quit ONLY in a named arrival QA world with gg.

Status gate passes; Structure stays red only on the known legacy exact CLI
ValidateSet and accepted5-color-tree expectations. Broader seeds/settings/mod
compatibility, forced native rollback, pickup-then-reload and multiplayer remain
production work. Scene review pending, not integrated/polished. After bounded
divot acceptance, next planned slice is Maw entrance/shallow area. Preserve
the two pre-existing dirty Localization and research files, and prior known
grove mismatch/background readiness failures.

## Historical checkpoint follows

## Current — September8: smooth flight accepted; resume pod checks

The user accepts Native-v3 pod appearance ("Very good design"). No more pod
redraw; remaining manual furniture/multiplayer and production-divot gates stand.
Current request is the sudden change in scenery speed at each height cap.
User approves broad altitude easing, preserved layer depth and flexible exit
heights. Old15%/25%/50% caps below are historical, not current policy.

`WastesCameraProjection.FlightDisplacement` integrates a smoothstep response;
first10% ascent preserves low motion, then Close>Mid>Far rates through Space.
No timer/spring/fade, horizontal/art/spacing/terrain changes. All410 candidate
Content PNGs match the previous pod build. Actual old source fails the new
slope regression;179811 smooth,1731 depth,5799 fixed-section checks and10 source
mutations pass. All9 native cases pass at2560x1369: ascent/descent/mid-hold-return,
both running-camera traces, both diagonals and both ground pans. All757 selected
records replay through independent height, running and modular validators;
2 positive/7 negative native-trace controls pass. User follow-up: "works great".
The bounded motion-comfort gate is accepted; do not reopen camera/art tuning.

Installed final diagnostic package SHA256:
`8DC9FB30C538BC091ECADCF9FA8C9163927FC554C2D7DBA5A2E20903148BEB36`.
Mirror: `C:/Users/max_h/AppData/Local/Temp/ApogeanTmlBuild/9d0935fe17494a42ab5fbabd2624c8e1/apogean`.
Zero warnings/errors. Backup of pre-change package, saved QA world and gg:
`C:/Users/max_h/AppData/Local/Temp/ApogeanSmoothFlightBackup-7bc6447bb36c4019b05fd04c03ba9971`.
Evidence and replay: `Art/Validation/WastesSmoothFlight-2026-09-08/README.md`.
First incomplete Close trace retained red; diagnostic now observes modular
tops before culling, including offscreen holds. No renderer change on that retry.

Do not claim all backgrounds complete: legacy readiness remains red (26 small
V0 images/old last-row stretch). Grove reload remains failed at87B951FE... vs
43FE618C..., with no rebuild/rebaseline. Only gg/Apogee Native Visual V3 opened.
Do not touch the user's Localization/en-US_Mods.apogean.hjson or
RESEARCH_TMODLOADER_WORLD_SYSTEMS.md. Next: resume the pod's remaining behavior
gates and then bounded divot, not another art loop. No new tests, build or game
control were needed to record this user acceptance.

Handoff state: camera sequences expired and restored control. Used the existing
pod `day` view only to leave gg beside the accepted pods in daylight; no build
or mechanical retest. Its within-session grove guard passes, not reload. No
pending request at that handoff. The user subsequently accepted flight; current
client state has not been re-inspected. Do not restart or take control again
without a relevant need.

## Latest — September8: Native-v3 flat-footing repair

User likes v2's coarse body/hatch but rejects floating/curved ground contact.
V2 had only18 bottom contact pixels; old minimum12 validator missed the symptom.
New Test-ArrivalPodFooting.ps1 rejects v2 and a2x2 contact-hole defect, accepts v3,
checks7040 upper pixels unchanged, exact replay, overwrite/Content refusal/pins.
V3 takes only an interior-metal strip from built-in imagegen edit; the model's
opaque matte and altered upper body are NOT used. Bottom8rows now have64px flat
contact. Native DrawYOffset0->2 seats it into measured0..2px soil edge recesses.
Same5x6 atlas/anchors, item and spawn behavior. No surrounding art changes.

Installed isolated package SHA256:
`7F8DF90A2C012B26D4FFDAFE0BCFB2AB7C59AD3B13DCA51443BA9B8D5C9DB18B`.
Mirror: `C:/Users/max_h/AppData/Local/Temp/ApogeanTmlBuild/f9152dd8706c4574b6afeddb2526ec45/apogean`.
Backup after safe QA save:
`C:/Users/max_h/AppData/Local/Temp/ArrivalPodFootingBackup-e72620a22680427892ed713fb1a16c65`.
Build0warnings/errors;408 other Content PNGs unchanged. V1/v2 pipeline and Status
gate pass. New native58-check matrix passes with2px registered offset; real
save/exit/reopen retained exact pod checkpoint273B29B0... and empty fourth slot.
Evidence: `Art/Validation/ArrivalPod-Footing-2026-09-08/`.
Day, player-overlap and night views captured; returned to daylight and left gg
beside the pods. The exact selected log passes the58-check validator. Its reload
record precedes the matrix; no second post-matrix reload claim.

Grove reload remains FAILED, not repaired/rebased. This reopened session's actual
digest is43FE618C... against the old expected87B951FE... (previous session actual
was65A5229C...). Do NOT say its digest was stable across reload. Pod commands
guard it unchanged only within the current session. No cause claim for the
older grove mismatch. Regular worlds untouched.

Next: user native footing judgment; remaining manual inventory/pickup/re-placement,
actuator/sloped-anchor and multiplayer cases, then bounded new-world divot.
Do not redraw approved body, implement relay or start Maw. Preserve user's two
unrelated files (Localization/en-US_Mods.apogean.hjson, RESEARCH_TMODLOADER_WORLD_SYSTEMS.md).

## Earlier September8 — Native-v2 in game for user judgment

User explicitly needs the in-game view before a yes/no. Installed the exact
Native-v2 candidate through the opt-in isolated build, NOT production Content.
Build: zero warnings/errors. Package SHA256:
`CF11FF1CBA3189F8F2BD8612A90375AC3A1353D26013924D7BDA8B3ED48AFCE8`.
Mirror: `C:/Users/max_h/AppData/Local/Temp/ApogeanTmlBuild/ddc579a60d304e4981aed877517dbf24/apogean`.
Pre-run package + QA world + gg backups:
`C:/Users/max_h/AppData/Local/Temp/ApogeanArrivalPodV2QA-5d7b55d282504d6a86c374043be0cbfc`.
Only two pod PNGs differ from previous mirror; 408 other Content PNGs match.
No runtime C# or production Content changes. Build selector pins v1 and v2
explicitly, requiring v2's 2x2 grid and exact hashes.

Opened gg / Apogee Native Visual V3 only. Existing pod checkpoint reload PASS;
three objects and empty fourth slot preserved, no fixture rebuild. Actual
day/player-overlap/night and final daylight captures:
`Art/Validation/ArrivalPod-v2-2026-09-08/`. day-detail.png is a 1x native screenshot
crop, NOT the old context-scale.png. Left the game running in daylight beside
the pod so the user can judge. Do not restart or take control again needlessly.
Middle copy is a blue paint test. Platform is a fixture, NOT the impact divot.

The pre-existing grove reload mismatch remains open (87B951FE... vs65A5229C...),
guarded unchanged across pod view commands; no repair/rebase. Native-v1's 58
mechanical tests are historical, not rerun this texture-only review. No pod
crash observed. Broader Structure gate retains its two legacy red expectations.

NEXT: user native appearance judgment, then remaining manual inventory/pickup/
re-placement, actuator/sloped-anchor and multiplayer checks, before bounded
new-world divot. Do not require another offline approval. No regular-world
retrofit, spawn edit, relay or Maw implementation. Preserve both unrelated
user changes: Localization/en-US_Mods.apogean.hjson and
RESEARCH_TMODLOADER_WORLD_SYSTEMS.md. Earlier checkpoints below are history.

## Earlier September 8 — crash direction agreed; exact coarse candidate ready

User's newest "yes agreed" approves the damaged-but-survivable arrival and
small generated divot, not automatic installation. Native-v2 is now in
`Art/Candidates/ArrivalPod-v1/Native-v2/`: 80x96 canvas, bottom-aligned 80x80
silhouette, 19 colors, strict 2x2 clusters, transparent 90x108 tile atlas.
`preview.png` shows actual 1x/4x pixels; `context-scale.png` shows an unchanged
old native player capture beside the new offline candidate at approximate
matching screen scale. Do not call that a live test of Native-v2.

`New-ArrivalPodNative.ps1 -Variant Native-v2` reuses the existing exact-mask
exporter. The generated selection had false interior holes; three A5-specific
joins + enclosed-hole fill preserve original kept RGB. No crater is baked
into the movable item. Uniform pixel size is now explicitly tested: the old
valid Native-v1 intentionally fails the new coarse-grid requirement. Full
pipeline passes both variants, 11 old defect cases + 3 coarse controls,
reproduces 5 original images and 6 coarse images/2 reports, preserves 19 inputs.
No Content, runtime, installed package, game, QA world, worldgen or relay change.

NEXT: review this exact candidate -> explicit isolated Native-v2 package
support + live appearance -> remaining manual pickup/inventory/re-placement,
actuator/sloped-anchor and multiplayer -> bounded new-world divot. Build tool
still pins ONLY Native-v1; do not silently replace it or rebuild the QA fixture.
Preserve the user's unrelated Localization/en-US_Mods.apogean.hjson and
RESEARCH_TMODLOADER_WORLD_SYSTEMS.md changes. See Native-v2 README for hashes,
mask prompt, caveats and validation. The following September 7 entry is history.

## September 7 — pod behavior passed, coarse damaged art review pending

Native-v1 furniture/item is implemented in an opt-in isolated package. Build
clean; 58 native programmatic checks and save/reload passed. Screenshots,
commands, hashes, original failed checkpoint and limitations are in
`Art/Validation/ArrivalPod-2026-09-07/README.md`. Preserve that evidence.
The save-test defect was hashing ordinary soil frames; V2 hashes persistent
state and still detects pod-frame, paint and support damage. Fixture was not
rebuilt. The known older grove mismatch remains untouched.

IMPORTANT newest user correction: Native-v1 is too detailed. User accepts A4's
coarser pixel treatment, then asks for more grit, visibly broken components,
and ground that looks impacted by the pod. `Art/Candidates/ArrivalPod-v1/A5-ImpactReview/`
contains the shown review-only edit and prompt. Do not install it or claim it
is native-scale/transparent. Keep the chunky style; no return to tiny scratches
and speckles. Broad soot, chips, bent hatch, broken roof and shallow passable
impact divot; movable pod art must be separate from actual terrain/debris.

Next: approval of A5 direction -> exact native export/player comparison ->
appearance recheck and remaining item/manual/actuator/multiplayer tests ->
bounded new-world spawn placement. No worldgen/divot/relay yet. Accepted Wastes
assets and motion remain unchanged (408 pre-existing PNG hashes identical).
Installed QA package SHA256 starts 62F57811; same required scenery inputs plus
ArrivalPodCandidateDirectory. Existing pod fixture is at (4476,540,64,24),
floor559, in gg / Apogee Native Visual V3. Do not rebuild it or open regular
worlds. Backups and exact commands are in the evidence README. UI session was
saved/quit after testing. Runtime tests passing do not approve rejected art.

Keep the user's unrelated Localization/en-US_Mods.apogean.hjson and
RESEARCH_TMODLOADER_WORLD_SYSTEMS.md modifications unstaged and intact.

## Historical checkpoint below — superseded by the section above

## Current direction — Wastes approved; arrival pod next

User: "looks great move on now". Wider spacing is accepted. Do not reopen
approved Close/Mid art, motion or spacing; occasional overlap is deferred.
Next slice is starting-area pod design, not additional background polish.
User answered the removal question: movable immediately; relay unlocks later.
The pod is non-solid furniture, recovered intact with a normal pickaxe, and
never moves world/bed spawn. Proposed geometry and installation gates are in
`ARRIVAL_POD_CONTRACT.md`. No runtime or save/world-generation change in this
design pass. Preserve the installed FE1B5E8B package and regular worlds.

User's targeted art feedback: first board's design/detail + second board's
chunky pixels. Both were revisions of Design A; label the blend **A3**. Preserve
the first architecture and mechanical parts instead of smoothing or collapsing
them into a featureless mosaic. The old `rejected-detailed-board.png` filename
records technical export rejection, not the user's preferred design judgment.
Keep originals. Native assembly and isolated fixture remain later approval gates.
Board annotations/person are illustrative, never measured scale/frame evidence.
Full prompts and limits are beside the PNGs under `Art/Candidates/ArrivalPod-v1`.
No silent installation or board-to-Content crop. No pod build/live claim.

A3 is now explicitly accepted ("perfect"); do not restart its art cycle.
`Art/Candidates/ArrivalPod-v1/Native-v1/preview.png` shows a separate statically
checked 80x96/20-color fit and exact 4x previews. Thirty 16x16 cells round-trip
through the 90x108 padded sheet; alpha, contact, connectivity and mutation
controls pass. Mask/color remain separate; two narrow hinge mask corrections
preserve attachment. Size/palette fitting is LOSSY, not lossless source art.
Native-size appearance review is pending; no Content/C# tile/worldgen changes,
build, live fixture or installation. Keep the accepted game package unchanged.
Native-v1 README and tools record reproduction and rejection controls.
Pod and shared furniture checks pass, including CLI negative/reproduction
controls. Structure gate remains RED from unchanged WorldVisualIntegrity
checks (exact command-list literal; existing 5-color tree versus minimum8).
Status gate passes. Do not confuse those broader failures with a pod-frame
failure, repaint approved trees, or claim the whole structure suite passed.

## Latest review — foreground accepted; sparser Mid installed and checked

User loves the farther-back foreground. Do not reopen depth/art/height tuning;
occasional overlap with Mid is minor deferred polish. New request only: fewer
Mid pieces, larger gaps. Source now uses a10,000px period/2,000px landmark
interval/1,150px quiet offset (32% lower density;459–638px gaps). All art,
sizes, ordering and parallax remain unchanged. Static layout176 cases and four
negative CLI controls pass, as do accepted foreground regressions.
The first install was blocked by TML003. After the user said "closed", the
same-input package installed with zero warnings/errors; SHA FE1B5E8B284FE8A21CF3029964967997396D8226F32F1A9D5E0F0642655669FA.
Native QA with gg now passes all eight planned cases at2560x1369: run-flat,
run-diagonal, both ground pans, both diagonals, ascent/descent. Zero modular
matrix/depth/count failures;15 stable Close sections in each pan. All three
live validators replay the1,639 exported QA records successfully. Nine raw
window captures retained, including terrain-obscured frames clearly labelled.
Earlier coverage failures do not reproduce in this bounded matrix. No ordinary
world opened; QA saved and main menu verified. Details, backup and mirror:
`Art/Validation/WastesMidSpacing-2026-09-07/README.md`.
User spacing review is now accepted, as is the foreground. Full
viewport/routing/performance, grove mismatch and production gates remain open.
Do not rebuild the preserved grove. Small exporter repair includes PROJECTION
SAMPLE records; regression was red then green, and the Background gate includes
it. Tools only, no further runtime/art change. Earlier sections below are history.

## Latest trial — foreground depth, not anchor smoothing

User clarified the combined diagonal movement and perceived closeness are the
problem: push foreground back toward Mid while remaining a little nearer.
Close now .20 horizontal/.06 vertical, capped at15% ascent. This intentionally
supersedes the old tile-speed Close contract. Mid25%/Far50% stay unchanged.
All artwork is preserved; fixed sections project terrain relief through .06.
New small-rise regression fails on the old21.33px response and passes at .96px.
New camera-only `run-flat`/`run-diagonal` traces measure submitted native pixels;
do not describe them as physical-running or user comfort acceptance. Current
evidence and remaining native outcome: `Art/Validation/WastesForegroundDepth-2026-09-07/README.md`.
Build passes zero warnings/errors. Preserve earlier coverage failures until the
complete new matrix passes, and never rebuild the mismatched QA grove.
Installed SHA0391BE79B2AB55861DD507C57BEF03DF00D6AA3DD7D9121C97E6FF5E521BCBF9.
QA saved, menu verified, game exited and restarted. Desktop attachment then
failed twice with `foreground window did not report a process id`; stop UI
input. User was asked to open QA with gg. No post-build native proof or screenshot
yet; use the README fixture order when available. Trace-validator synthetic
controls pass, but are not native records. Previous failed live status retained.

## Latest tuning — lower Mid/Far flight ceilings

User clarified maximum-height caps should be lower, NOT that ground scenery
should move down. The intervening interpretation was superseded before any
ground-placement edit. Current trial: Mid25%, Far50% of the existing ascent
reference, preserving base composition and Close anchors. Native/build outcome
is recorded in `Art/Validation/WastesLowerCaps-2026-09-07/README.md`.
The older Close low-view depth failure remains independent and blocks promotion.
The package builds with zero warnings/errors. Both complete native flight traces
pass the lower-cap policy at 2560x1369, with no height fade; three before/after
viewpoints confirm unchanged ground placement and earlier geometric exit. The
README retains raw window captures and sanitized telemetry. Exact fractions are
trial tuning awaiting user review, not permission to restart accepted artwork.
Both full diagonal sweeps retain 21 stable anchors and pass height arithmetic,
but the modular count/depth aggregate still fails (41 right, 42 left). Do not
call the full fixture passed. Six unedited screenshots and 1,602 after-build
allowlisted records are retained. QA saved and native main menu verified.

## Latest runtime correction — stable foreground and positional height caps

New standing permission: reusable engine/tooling can become a separate dependency
mod when technically justified. Recorded in DESIGN_BIBLE and AUTHORING_WORKFLOW.
Do not create a dependency merely to address this camera bug; no library or new
Workshop publication was made this turn.

User reports bouncing Close geometry and requests height locking instead of
altitude transparency for Mid/Far. This supersedes earlier opacity acceptance.
See `Art/Validation/WastesHeightLock-2026-09-07/README.md` for the earlier contract,
tests and native status. Each absolute Close section now samples saved terrain
at its own fixed X; Mid/Far originally stopped at1/3 and70% ascent, then left the
screen through ordinary world motion. No new art or global purification counter.
User has approved native tests only in Apogee Native Visual V3. Preserve the old
grove digest mismatch and unrelated localization/research edits. Do not promote
from historical opacity results or restart an accepted-art redesign loop.

Native completion: at2560x1369 both horizontal sweeps have21 stable Close
sections, zero anchor/height failures. Ascent/descent pass positional exit and
return, without altitude fading. **Full fixture rejected**: modular gate records
15053 right-pan and10342 left-pan failures, including demonstrated shallow Close
bottoms on lower viewpoints. No accepted PNG changed. Installed QA package builds
cleanly. Saved QA and visually verified main menu. Next bounded task is visible
low-camera depth coverage/cave handoff, preserving the approved architecture;
then rerun the whole sweep, not just its passing flight subset. Evidence README
contains hashes, screenshots, traces and explicit scope. Ordinary-world and
multiplayer promotion remain blocked. This correction takes priority over the
earlier vegetation-authoring next step below.

## Latest decision — vegetation style approved; Maw method reuse specified

User: "yes that looks great" and the same method can show Maw sleep/awakening.
Record vegetation-style acceptance, not runtime texture approval. Do not re-ask
that style choice. Registered plant-only authoring can proceed on the original
city; source sizes/masonry drift and the full-green endpoint remain separate gates.
`MAW_DORMANCY_VISUAL_DIRECTION.md` specifies stable Maw base + organ/light/detail
layers, dormant versus awake. Existing Matriarch/Hardmode triggers remain unchanged;
sleep does not purify tiles or credit Wastes recovery. Suggested pulses/brightness
still need art/timing review. Design-only, no jump ahead of Wastes/starting-area order.

## Earlier follow-through — reclamation concepts and offline calculator

After `go`, made `Art/Candidates/WastesReclamation-v1/`: original city crop plus
early/denser vegetation studies and exact built-in prompts. Only offline art;
no tModLoader takeover, restart, world edit, runtime code or Content PNG change.
Early actual2115x743; denser2116x743; original crop1458x512. Both studies fail the
real replacement gate for changed dimensions and no alpha, and visibly repaint
masonry. They are references only. Do not install, rescale into production or
describe them as native-size invariant-preserving art. Denser needs more living
ground/leafy trees before it represents100% recovery. Style is now accepted;
registered production assets and native review remain pending.

`Tools/Get-WastesReclamationProfile.ps1` is a draft offline calculator from
supplied counts. Close=1-(1-R)^3, Mid=1-(1-R)^2, Far=R. 1227 assertions pass via
`Tools/Test-WastesReclamationProfile.ps1`, including four invalid-count rejection
cases. No world classifier, save/network path or pixel-coverage proof. Updated
`WASTES_RECLAMATION_DIRECTION.md` with proposed fixed eligible surface measure,
unknown/migration/mining/reinfection policies and explicitly open decisions.
Status stays specified; counting/curves and native assets remain unapproved.

Next: author registered plant-only overlays in the accepted style; settle
the surface-plot policy before world-counter coding. Do not restart accepted ruin
architecture or confuse historical local fallback proof with global reclamation.
The existing cave/performance checks below are unaffected.

## Previous user clarification — reclamation design, not another runtime test

The user wants the same Wastes city/roads/cliffs gradually covered in living
plants and trees, driven by world-wide restoration. Do not treat the native
forest fallback as the final artwork or the local-only policy as the target.
`WASTES_RECLAMATION_DIRECTION.md` records intent and recommendations. User has
confirmed Close → Mid → Far with overlapping recovery. The whole Far city gets
sparse greenery early and denser growth as global recovery advances; no spatial
wipe or waiting for Mid to finish. Settle world counting and response curves
before coding; do not re-ask the accepted order. No thresholds, counter implementation,
new vegetation art or revised schedule is silently approved. Architecture stays
fixed; all historical QA passes retain only their original fallback scope.

## Earlier — completed native routing pass

User said go; continued the accepted four-building Wastes Mid bank without
redrawing any art. Fresh native2560×1369 unforced Wastes/mixed/green hysteresis,
real PureSpray conversion/fades, Jungle priority after restoration and return
to Wastes all pass.35 allowlisted records,8 engine panoramas,4 exact native
crops and the spray report/CSV are retained in
`Art/Validation/ForestRestoration/2026-09-07/README.md`.

One QA harness repair: restoration/Jungle requests retain the saved grove
checkpoint instead of clearing it. New pure placement keeps the entire fixture
and isolation/spray margin away from its bounds;72 placements and2 injected
bad-output controls pass.9 native guards preserve the same expected/actual
grove pair. The old87B951FE… / EB1D7019… reload mismatch still exists and was
NOT reset or bypassed. No fresh grove reload approval or complete clean-log claim.

0warning/0error isolated QA build, same six candidate inputs as September6.
Installed SHA256:9C15F8C50ADE16D7517CB14A0BBFC63BA7595D084E6102C50BE8189671A17F29.
Build mirror and backup paths are in the evidence. Native main menu verified
after saving only Apogee Native Visual V3 / gg at13:33:11 local. No regular
world, production Content artwork, routing thresholds or settings changed.
The artificially elevated restoration platform is not natural-ground alignment
proof. Green is the existing native/resource-pack Forest fallback, not authored
reclaimed ruins; Jungle uses its existing V0 scenery, not a newly accepted family.

Next bounded checks: visible cave handoff and measured performance. Then remaining
viewport/biome/persistence/MP coverage before ordinary-world promotion. All four
building TOPS are accepted and fixed; deep joins/spacing retain separate review.
Raw36.74MiB bank is not an approved aggregate budget or GPU measurement. Keep
Wayfinder#11 OPEN. No automatic regeneration or grove rebuild. Wastes mobs follow
the baseline; do not expand to new biome/faction production from routing proof.

Preserve unrelated dirty Localization/en-US_Mods.apogean.hjson and
RESEARCH_TMODLOADER_WORLD_SYSTEMS.md; exclude them from this checkpoint's commit.
Prior native movement/Space/lighting evidence remains at
`Art/Validation/WastesMidDepth-2026-09-06/README.md`.
