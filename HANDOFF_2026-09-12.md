# Maw eleven-hour pass — technical resume checkpoint

Read MAW_REVIEW_2026-09-12.md first for the short player-facing review.
Full chronological record: MAW_AUTONOMOUS_PASS_2026-09-12.md; evidence O–AC is
linked there and under Art/Validation. Later notes supersede September11's
NEXT_SESSION pause/build advice without erasing that historical record.

**Paused at03:59:44UTC September13**, before the04:46:58UTC cap, at the
review/actual-placement boundary. The existing heartbeat is PAUSED. Do not
resume unattended development or computer control until the user asks. This
was the maximum authorized window, not a claim that11 hours elapsed.
Implementation/evidence checkpoint6a37417 is pushed; Wayfinder#26 is updated
and remains OPEN. This final note records the safe pause, not a new feature.

## Current environment

Repo: `C:/Users/max_h/OneDrive/Documents/My Games/Terraria/tModLoader/ModSources/apogean`.
PrismLauncher is not this repository. The authorized window ends
**2026-09-13 04:46:58UTC**; never extend it or restart unattended work afterward.

tModLoader was normally saved at03:18:19–20UTC, then closed. Final read-only
check at03:48UTC:16 existing canonical system records unchanged against pre-AA.
At03:49UTC no Terraria window or queued request was present. No live test or
headless server remains running. Do not open aga or GOLB for tests.

Installed QA package SHA256:
`2450BDD240BB338F463578C0562652A25E8D2FB9F4512A3B82898851643E6343`.
Retained AA mirror:
`C:/Users/max_h/AppData/Local/Temp/ApogeanTmlBuild/263a8177b3e042e1a895634166f4d157/apogean`.
Later AB/AC changes are scripts/docs only; no package replacement.

Current anatomy is the **v1** bank, verified against actual AA mirror bytes,
not September11's alternative v2 command. Rib atlas:
`AB8CA02B16EEDB9768469CB5525FBAE9C6C13A6BC8D16A7018A8341444E818C2`.
The v2 alternative remains separately preserved, not accepted as current.
If a subsequent source change needs installation, close the client after a
native save and use the explicit matching preset:

```powershell
& ./Tools/Build-CurrentQAPackage.ps1 -PackedMawPreview -MawAnatomyStudy -MawCaveBackdropStudy -MawAnatomyCandidateDirectory 'Art/Candidates/MawRibSurface-v1/Native-v1' -KeepWorkspace
```

Code-only checks use Compile-ApogeanOnly.ps1 or the content gate's -Build.
Never use bare root dotnet build: the default tML target installs a package.

## Keep the saved specimens

| Specimen | Bounds in tiles | Current invariant |
|---|---|---|
| Shallow V1 |1116,180 /112×104|11648 cells,8 clusters; no rebuild|
| Separate contour study |1116,60 /124×82|10168 cells,592 solid; no rebuild|
| Earlier anatomy |7396,80 /100×76|Existing specimen, not a new reservation|
| Hanging-fiber exhibit |7396,176 /100×20|39 observed vs42 expected remains RED|
| Amber rooms |7276,216 /100×52|Saved light comparison, not a global-state change|

Shallow actual state:
`24163E65FC677E84DA67BBE7DC7E1535CEBB4096AE3326516418EC0A6213F5A5`.
Do not confuse it with creation/plan identity F3CC4D2B….
Contour state:
`6F7998FFECA9D342C28B3A7FC2F8E319FE3AB73598956BB221945EF35AB4B4DF`.
The nine known locations/nine absent locations count used by scratch probes is
not a claim that18 fixtures survive. Sixteen saved-system records is a different
inventory. Older `all17` strings were misleading and are explicitly corrected.

The old native grove expected87B951…/latest actualC6A92… mismatch remains RED.
Never regrow/rebuild/rebaseline it. Solar retaliation reproduced a vine-cut
mechanism in scratch but did not establish the cause of the original loss.

## Resume order

1. Review existing native rib/lighting captures with the user. Use Maw QA Plain
   in Apogee Native Visual V3 for clean held views; normal gg equipment includes
   retaliation. The contour candidate removes notches, not the internal bands.
2. For a native contour view, issue one Request-MawRibContourValidation `-Case
   medium`, wait for actual completion, then `inspect-on` if a diagnostic light
   is needed. Turn it off/release before judging natural light. Never `build`.
   Shallow view commands are Request-MawShallowValidation `rib1`, `rib2`, `rib3`,
   `pocket`, `top`; `play` releases the held camera for that existing scene.
3. Finish actual ordinary-rope placement: gg needs an existing Rope hotbar stack.
   X refused before setup because none was present. First vanilla and no-support
   controls, then rib/cap/wall/diagonal/far. Publish one exact request such as
   `maw-rope-itemuse-vanilla` through Publish-QARequest; await native result and
   independently validate it. No grants, refunds or invented placement pass.
4. Separately test manual pickup/placement and dropped-item rendering. Y proves
   native mine/API-replace/mine bindings; AA proves only native inventory hooks.
   V proves preplaced-rope ascent, not placement or whole-route return.
5. Review a practical return route and natural-light hazard readability before
   promoting the new contour into the old shallow scene or opt-in generation.
   Do not mutate/rebaseline shallow V1 to make tests green. A new revision needs
   its own saved specimen and evidence. No automatic safe staircase or ropes.

The dry cave overlay is parked for flat/banded art and wet-boundary work.
Generic HD V0 multi-biome production readiness remains RED; accepted Wastes
camera behavior is not being reopened solely by that legacy asset-size gate.
No new enemy/boss/acid/dependency branch before the agreed foundation review.

## Safety and evidence

Only disposable QA scope is authorized. Earlier aga was opened during an
interrupted UI sequence, then saved/exited with the user's permission; no QA
commands were run there. Do not claim it was never opened.

All Request*.ps1 wrappers now use closed-file atomic publication. A pending
command is preserved; do not delete it to force a new one. Observe fresh native
completion, not sender stdout. Native save is `qa-save-and-quit`; verify menu and
the actual save log. Never rely on repeated Escape presses that do not register.

Final regressions: Art/Validation/MawPassGate-2026-09-13. Thirty-four archived
native reports replay consistently, including retained failures/partial runs.
All + compile-only is RED only on BackgroundProductionReadiness; the full child
output capture provides detailed results. No art-status promotions.

Unrelated dirty files must stay unstaged and unchanged:
Localization/en-US_Mods.apogean.hjson (SHA256 0F350D75…);
RESEARCH_TMODLOADER_WORLD_SYSTEMS.md (SHA256 4911AB73…).
